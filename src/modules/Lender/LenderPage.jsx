import React, { Component } from 'react';
import LenderContent from './components/LenderContent';

export default class LenderPage extends Component {
    render() {
        return (
            <LenderContent />
        );
    }
}
